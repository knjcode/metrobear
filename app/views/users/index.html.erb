<% provide(:title, 'メンバ一覧') %>
<h1 class="left">メンバ一覧</h1>
<hr>

<%= will_paginate previous_label:"≪", next_label:"≫" %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate previous_label:"≪", next_label:"≫" %>
