<li>
  <a href="/users/<%= user.nickname %>"><%= user_avatar user %><%= user.nickname %></a>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
  <p>訪問済み駅数： <%= user.station_count %></p>
</li>
